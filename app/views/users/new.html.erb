<h1>My Page</h1>

<!-- 試用のために仮設置中 -->
<%= link_to "Log out", destroy_user_session_path, method: :delete %>

<%= form_for(@user) do |f| %>
	<div class="leftside">
		<h1>User info</h1>
		<span>picture</span>
		<p>"name"</p>
		<%= @user.name %>
		<p>introduction</p>
		<p>hello.</p>
		<%= link_to "edit menu", edit_user_path %>
		<h1>New book</h1>
		<label>title</label>
		<%= f.text_field :title %>
		<label>body</label>
		<%= f.text_area :body %>
		<%= f.submit%>
<% end %>
